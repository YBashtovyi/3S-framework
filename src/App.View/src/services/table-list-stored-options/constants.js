
export const TABLE_LIST_OPTIONS = 'TABLE_LIST_OPTIONS'

/**
 * Key, which is used for storing filters from the table list
 */
export const FILTER_OPTIONS = 'FILTER_OPTIONS'

/**
 * Key, which is used for storing filters dictionary (key and value of items, which are already enabled)
 */
export const ACTIVE_FILTERS_OPTIONS = 'ACTIVE_FILTERS_OPTIONS'

/**
 * Key, which is used for storing pagination information from the table list
 */
export const PAGINATION_OPTIONS = 'PAGINATION_OPTIONS'